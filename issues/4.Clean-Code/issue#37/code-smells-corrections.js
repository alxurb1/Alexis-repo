
/*
//MAGIC NUMBERS AND STRINGS

const DISCOUNT_VIP = 0.8;
const DISCOUNT_REGULAR = 0.9;
const TYPE_VIP = "VIP";
const TYPE_REGULAR = "REGULAR";

function calculateDiscount(price, customerType) {
  if (customerType === TYPE_VIP) return price * DISCOUNT_VIP;
  if (customerType === TYPE_REGULAR) return price * DISCOUNT_REGULAR;
  return price;
}


//LONG FUNCTION

function process() {
  validate();
  const total = calculate();
  save(total);
  notify();
}



//DUPLICATED CODE

function area(w, h) { return w * h; } 


//LARGE CLASS

class UserService { addUser() {} removeUser() {} }
class OrderService { createOrder() {} cancelOrder() {} }
class PaymentService { processPayment() {} }



//DEEPLY NESTED CONDITIONALS

if (!u) return "No User";
if (!u.active) return "Inactive";
if (u.role === "admin") return "Access";
return "Restricted";

//COMMENTED-OUT CODE

function total(p) { return p.reduce((a, b) => a + b); }


//INCONSISTENT NAMING

let price = 10, items = [1, 2, 3];
function calculateTotal(price, items) {
  return price * items.length;
}
*/