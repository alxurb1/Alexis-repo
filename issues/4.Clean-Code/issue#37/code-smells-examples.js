/*
//MAGIC NUMBERS AND STRINGS

function calculateDiscount(price, customerType) {
  if (customerType === "VIP") {
    return price * 0.8; 
  } else if (customerType === "REGULAR") {
    return price * 0.9; 
  } else {
    return price;
  }
}


//LONG FUNCTION

function process() {
  validate();
  calc();
  save();
  notify();
}


//DUPLICATED CODE

function area1(w, h) { return w * h; }
function area2(w, h) { return w * h; } 


//LARGE CLASS

class App {
  addUser() {}
  removeUser() {}
  createOrder() {}
  sendEmail() {}
  processPayment() {}
}


//DEEPLY NESTED CONDITIONALS

if (u) {
  if (u.active) {
    if (u.role === "admin") {
      return "Access";
    }
  }
}


//COMMENTED-OUT CODE

// function oldTotal() { ... }
function total(p) { return p.reduce((a, b) => a + b); }


//INCONSISTENT NAMING

let x = 10, y = [1,2,3];
function c(p, q) { return p * q.length; }
*/

