function calculateTriangleArea(base, height) {
  let area = base * height;
  let triangleArea = area / 2;
  console.log("The triangle area is: " + triangleArea);
}

let baseValue = 30;
let heightValue = 40;
calculateTriangleArea(baseValue, heightValue);
